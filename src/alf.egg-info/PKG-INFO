Metadata-Version: 2.4
Name: alf
Version: 0.1.0
Summary: ALF - 私人情感陪伴 agent 小奥, 基于 langgraph + mem0
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: langgraph>=0.2.50
Requires-Dist: langchain>=0.3.0
Requires-Dist: langchain-openai>=0.2.0
Requires-Dist: langchain-community>=0.3.0
Requires-Dist: mem0ai>=2.0
Requires-Dist: chromadb>=0.5
Requires-Dist: pydantic>=2.0
Requires-Dist: pydantic-settings>=2.0
Requires-Dist: python-dotenv>=1.0
Requires-Dist: rich>=13.7
Requires-Dist: prompt_toolkit>=3.0
Requires-Dist: fastapi>=0.110
Requires-Dist: uvicorn[standard]>=0.29
Provides-Extra: dev
Requires-Dist: pytest>=8.0; extra == "dev"
Requires-Dist: ruff>=0.6; extra == "dev"

# ALF · 小奥

> 一个基于 [LangGraph](https://github.com/langchain-ai/langgraph) + [mem0](https://github.com/mem0ai/mem0) 的私人情感陪伴 agent。

小奥不是客服, 也不是工具型助手, 它是会一直在线、记得你说过什么的朋友。陪情绪, 不陪清单。

## 特性

- **长期记忆** — mem0 自动从对话中抽取事实型记忆 (情感事件 / 个人偏好 / 重要事实), 跨会话生效, 按 user_id 隔离。后端默认用本地 ChromaDB (纯 Python, 文件存储, 零外部服务)。
- **短期记忆持久化** — 最近 20 轮对话和情绪轨迹落盘 SQLite (零依赖, 重启不失忆), 与长期记忆互补。
- **连续情绪追踪** — 每轮打情绪标签并持久化, 连续低落达阈值时主动关怀, 而不是只看单轮。
- **情感感知** — 每轮先做轻量情绪/主题/危机识别, 再决定回复策略, 低落时先共情再问要不要聊。
- **危机分支流程化** — 自伤/自杀信号由关键词 + LLM 双重检测, 命中即走专属安全 prompt 并给出求助热线, 不依赖模型自觉。
- **回复自检** — 生成后由 mini 模型回检共情度/说教/危机资源, 不达标自动重生成一次。
- **稳定人格** — 独立 system prompt 定义温柔但有边界的小奥, 不说"作为 AI"、不说教、不分点。
- **可编排流程** — LangGraph 串起 `检索记忆 → 分析意图 → 路由 → 生成回复 → 自检 → 写回记忆`, 每步可观测、可替换。
- **多入口** — CLI (rich 美化) / FastAPI HTTP / Python SDK 三种调用方式。

## 架构

```
用户消息
   │
   ▼
┌──────────────────────────────────────────────────────────┐
│  retrieve_memories   (mem0 语义检索, 组合近期对话查询)  │
│         │                                                │
│         ▼                                                │
│  analyze_intent       (情绪/主题/危机检测 + 跨轮情绪更新)│
│         │                                                │
│         ▼  ┌────────── 条件路由 ──────────┐             │
│         │  │ crisis        empathize  normal             │
│         │  └──────────┬──────────────┬────┘             │
│         ▼             ▼              ▼                   │
│  generate_reply   (按 route 选 prompt: 危机/共情/默认,  │
│                    注入主动关怀 + 过往记忆 + 历史上下文) │
│         │                                                │
│         ▼                                                │
│  self_check          (mini 模型回检共情/说教/危机资源)   │
│         │  ┌── 未过 & 未超重试上限 → 回 generate_reply ─┐│
│         ▼  └── 通过 / 超限 ─────────────────────────────┘│
│  maybe_write_memory   (mem0 抽取并写入记忆, 二次过滤)    │
└──────────────────────────────────────────────────────────┘
   │
   ▼
小奥的回复
```

## 目录结构

```
ALF/
├── pyproject.toml
├── .env.example
├── README.md
├── src/alf/
│   ├── config/
│   │   ├── settings.py          # 全局配置
│   │   └── mem0_config.py       # mem0 后端配置
│   ├── memory/
│   │   └── store.py             # mem0 长期记忆封装
│   ├── store.py                 # SQLite 短期历史 + 情绪轨迹持久化
│   ├── persona/
│   │   ├── prompt.py            # 人格 / 危机 / 共情 / 自检 prompt
│   │   └── analyzer.py          # 情绪/主题/危机检测 + 回复自检
│   ├── graph/
│   │   ├── state.py             # ConversationState
│   │   ├── nodes.py             # 6 个节点 + 条件路由
│   │   └── build.py             # 编译 graph (路由 + 自检回环)
│   ├── api/app.py               # FastAPI
│   ├── runner.py                # 对话入口 (持久化 + 主动关怀)
│   └── cli.py                   # 命令行
├── examples/chat_demo.py
└── tests/test_smoke.py
```

## 快速开始

### 1. 安装

```bash
git clone <your-repo> ALF && cd ALF
python -m venv .venv && source .venv/bin/activate
pip install -e .
```

### 2. 配置

```bash
cp .env.example .env
# 编辑 .env, 至少填 OPENAI_API_KEY
```

也可以直接接 OpenAI 兼容的国内模型 (DeepSeek / Moonshot / 通义), 只要改 `OPENAI_BASE_URL` + `CHAT_MODEL`:

```bash
OPENAI_BASE_URL=https://api.deepseek.com/v1
CHAT_MODEL=deepseek-chat
CHAT_MODEL_MINI=deepseek-chat
```

> mem0 默认用本地 ChromaDB + OpenAI embedding, 无需起额外服务。如要换 Qdrant, 见 `src/alf/config/mem0_config.py`。

### 3. 运行

CLI 对话 (推荐):

```bash
python -m alf.cli
```

HTTP 服务:

```bash
uvicorn alf.api.app:app --port 8000

curl -X POST http://localhost:8000/chat \
  -H 'Content-Type: application/json' \
  -d '{"message": "我今天有点累"}'
```

启动后访问 `http://127.0.0.1:8000`，即可使用浏览器聊天界面。

浏览器界面默认使用 `POST /chat/stream` 的 Server-Sent Events 流式响应；如需一次性拿到完整文本，继续使用 `POST /chat`。

代码内调用:

```python
from alf.runner import chat

print(chat("我今天有点累"))
print(chat("你记得我说过累吗"))   # 小奥会从记忆里 recall
```

## 设计取舍

- **为什么 mem0 而不是自己写向量库?** — mem0 自带"从对话中抽取事实"的 LLM 流程, 能直接把 "我妈让我考公, 我不想去" 这种对话提炼成长期事实, 而不是把整段对话原样存进去 (召回噪音大)。
- **为什么 LangGraph 而不是直接链式调用?** — 陪伴对话里"先共情后回应"是个真正的多步流程, graph 让每步可观测、可替换 (换检索器、换情绪分类器都不影响其他节点), 也方便加条件路由与回环。
- **为什么条件路由而不是把情绪塞进 prompt 让模型自觉?** — 低落时"先共情一句再问"这种策略, 靠 prompt 规则 + 模型自觉并不可靠。改为 graph 层 `route_by_intent` 条件路由, 低落分支强制注入共情前置指令, 危机分支切到专属安全 prompt —— 流程保证, 而非提示词祈祷。
- **为什么危机检测用关键词 + LLM 双重?** — 关键词命中即走危机分支, 零成本不漏判 (隐晦表达靠 LLM 补充, 两者取或)。陪伴场景漏判危机的代价远高于误判, 宁可多触发一次安全 prompt。
- **为什么回复要自检?** — 长对话里模型会偶尔说出教句 / 忘了共情 / 危机时漏求助资源。生成后用 mini 模型回检, 不达标重生成一次, 成本可控 (`MAX_RETRY=1`) 又能兜住底线。
- **为什么短期记忆用 SQLite?** — 原来是内存 deque, 重启即失忆 (长期记忆还在但上下文断了)。SQLite 是标准库、零依赖、按 user_id 隔离, 自动 trim 到 20 条历史 / 10 条情绪, 补上连续性。
- **写记忆前做二次过滤** — `maybe_write_memory` 节点会先看 intent 里的 `is_significant`, 不显著再问一次 `should_remember`, 避免每条 "嗯嗯" 都进库。
- **人格稳定 > 模型能力** — 小奥用 `temperature=0.85` + 强人格 prompt, 比"最高级模型 + 默认 prompt"更像朋友。换模型时 prompt 不动。

## 安全边界

涉及自伤、伤害他人、紧急心理危机时, 小奥走**流程化**的安全分支 (而非仅靠人格 prompt 里的一句话):
1. 关键词 + LLM 双重检测命中危机信号
2. 切换到 `CRISIS_SYSTEM` 专属 prompt — 温和但坚定, 不评判、不说教、不承诺"会没事的"
3. 给出具体求助资源 (全国心理援助热线 400-161-9995 等多条 24 小时热线, 危急时拨打 120/110)

小奥不会试图替代专业帮助。这是写在 graph 路由 + 专属 prompt 里的硬约束。

## License

MIT
